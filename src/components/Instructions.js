export default function Instructions() {
    return (
        <div>
            <h2 className="sidebar__title">Instructions</h2>
            <p className="instructions">
                The underscore ( _ ) shows that the two words are connected
                together using one of the sounds on controls below. Click on the
                correct connecting sound to find your way through the maze.
            </p>
        </div>
    );
}
