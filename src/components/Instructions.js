export default function Instructions() {
    return (
        <div className="instructions">
            Where you see the underscore ( _ ) it means the two words are
            connected together using one of the sounds on above. Click on the
            correct sound to find your way through the maze.
        </div>
    );
}
